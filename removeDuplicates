var removeDuplicates = function(nums) {
   let newArr = 0;
    for (i=0; i<= nums.length; i++) {
        if(nums[i] != nums[i+1]) {
            nums[newArr] = nums[i];
            newArr++;
        }
    }
    return newArr;
};
